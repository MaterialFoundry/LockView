<div>
    <p>
        {{localize "LOCKVIEW.CloneView.Hint_A"}}
    </p>

    {{formGroup pan}}
    <div class="form-group">
        <label>{{localize "LOCKVIEW.CloneView.Zoom"}}</label>
        <div class="form-fields">
            <select name="zoom">
                {{selectOptions zoomOptions selected=zoom}}
            </select>
        </div>
    </div>

    <hr>

    <p>
        {{localize "LOCKVIEW.CloneView.Hint_B"}}
    </p>

    {{#each users as |u|}}
        {{formGroup u}}
    {{/each}}

</div>